# Misconception reset cards

## Misconception

AI is a database.

## What is partly true

There is a reasonable concern or goal inside this statement.

## What is misleading

It collapses distinct layers such as model, product, retrieval, permissions, hosting, evaluation, and human oversight.

## Better mental model

Name the layer you mean, identify who controls it, and test whether that layer solves the actual problem.

## Question to ask next

What problem are we trying to solve, and which system layer would have to change?

## Misconception

A chatbot is the same as a model.

## What is partly true

There is a reasonable concern or goal inside this statement.

## What is misleading

It collapses distinct layers such as model, product, retrieval, permissions, hosting, evaluation, and human oversight.

## Better mental model

Name the layer you mean, identify who controls it, and test whether that layer solves the actual problem.

## Question to ask next

What problem are we trying to solve, and which system layer would have to change?

## Misconception

Uploading documents trains the model.

## What is partly true

There is a reasonable concern or goal inside this statement.

## What is misleading

It collapses distinct layers such as model, product, retrieval, permissions, hosting, evaluation, and human oversight.

## Better mental model

Name the layer you mean, identify who controls it, and test whether that layer solves the actual problem.

## Question to ask next

What problem are we trying to solve, and which system layer would have to change?

## Misconception

We need our own LLM.

## What is partly true

There is a reasonable concern or goal inside this statement.

## What is misleading

It collapses distinct layers such as model, product, retrieval, permissions, hosting, evaluation, and human oversight.

## Better mental model

Name the layer you mean, identify who controls it, and test whether that layer solves the actual problem.

## Question to ask next

What problem are we trying to solve, and which system layer would have to change?

## Misconception

Retrieval eliminates hallucinations.

## What is partly true

There is a reasonable concern or goal inside this statement.

## What is misleading

It collapses distinct layers such as model, product, retrieval, permissions, hosting, evaluation, and human oversight.

## Better mental model

Name the layer you mean, identify who controls it, and test whether that layer solves the actual problem.

## Question to ask next

What problem are we trying to solve, and which system layer would have to change?

## Misconception

A citation proves the answer is reliable.

## What is partly true

There is a reasonable concern or goal inside this statement.

## What is misleading

It collapses distinct layers such as model, product, retrieval, permissions, hosting, evaluation, and human oversight.

## Better mental model

Name the layer you mean, identify who controls it, and test whether that layer solves the actual problem.

## Question to ask next

What problem are we trying to solve, and which system layer would have to change?

## Misconception

Open-weight means private.

## What is partly true

There is a reasonable concern or goal inside this statement.

## What is misleading

It collapses distinct layers such as model, product, retrieval, permissions, hosting, evaluation, and human oversight.

## Better mental model

Name the layer you mean, identify who controls it, and test whether that layer solves the actual problem.

## Question to ask next

What problem are we trying to solve, and which system layer would have to change?

## Misconception

Agents are autonomous colleagues.

## What is partly true

There is a reasonable concern or goal inside this statement.

## What is misleading

It collapses distinct layers such as model, product, retrieval, permissions, hosting, evaluation, and human oversight.

## Better mental model

Name the layer you mean, identify who controls it, and test whether that layer solves the actual problem.

## Question to ask next

What problem are we trying to solve, and which system layer would have to change?

## Misconception

Prompt engineering is an AI strategy.

## What is partly true

There is a reasonable concern or goal inside this statement.

## What is misleading

It collapses distinct layers such as model, product, retrieval, permissions, hosting, evaluation, and human oversight.

## Better mental model

Name the layer you mean, identify who controls it, and test whether that layer solves the actual problem.

## Question to ask next

What problem are we trying to solve, and which system layer would have to change?

## Misconception

The largest model is always the best model.

## What is partly true

There is a reasonable concern or goal inside this statement.

## What is misleading

It collapses distinct layers such as model, product, retrieval, permissions, hosting, evaluation, and human oversight.

## Better mental model

Name the layer you mean, identify who controls it, and test whether that layer solves the actual problem.

## Question to ask next

What problem are we trying to solve, and which system layer would have to change?

## Misconception

A strong demonstration proves deployment value.

## What is partly true

There is a reasonable concern or goal inside this statement.

## What is misleading

It collapses distinct layers such as model, product, retrieval, permissions, hosting, evaluation, and human oversight.

## Better mental model

Name the layer you mean, identify who controls it, and test whether that layer solves the actual problem.

## Question to ask next

What problem are we trying to solve, and which system layer would have to change?

## Misconception

More automation always creates efficiency.

## What is partly true

There is a reasonable concern or goal inside this statement.

## What is misleading

It collapses distinct layers such as model, product, retrieval, permissions, hosting, evaluation, and human oversight.

## Better mental model

Name the layer you mean, identify who controls it, and test whether that layer solves the actual problem.

## Question to ask next

What problem are we trying to solve, and which system layer would have to change?

## Misconception

A school-specific interface means a school-specific foundation model.

## What is partly true

There is a reasonable concern or goal inside this statement.

## What is misleading

It collapses distinct layers such as model, product, retrieval, permissions, hosting, evaluation, and human oversight.

## Better mental model

Name the layer you mean, identify who controls it, and test whether that layer solves the actual problem.

## Question to ask next

What problem are we trying to solve, and which system layer would have to change?

## Misconception

Fine-tuning is the best way to add current institutional knowledge.

## What is partly true

There is a reasonable concern or goal inside this statement.

## What is misleading

It collapses distinct layers such as model, product, retrieval, permissions, hosting, evaluation, and human oversight.

## Better mental model

Name the layer you mean, identify who controls it, and test whether that layer solves the actual problem.

## Question to ask next

What problem are we trying to solve, and which system layer would have to change?

## Misconception

Human in the loop automatically makes a system safe.

## What is partly true

There is a reasonable concern or goal inside this statement.

## What is misleading

It collapses distinct layers such as model, product, retrieval, permissions, hosting, evaluation, and human oversight.

## Better mental model

Name the layer you mean, identify who controls it, and test whether that layer solves the actual problem.

## Question to ask next

What problem are we trying to solve, and which system layer would have to change?
