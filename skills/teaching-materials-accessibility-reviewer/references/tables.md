# Tables

Check for real header rows, simple structure, no merged cells where avoidable, no blank cells used only for spacing, clear row and column labels, captions or introductory text, data-table use rather than layout use, and content that remains understandable when read linearly. For complex tables, recommend splitting, normalizing, or converting to structured lists.
