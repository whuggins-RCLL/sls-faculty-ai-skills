# SLS AI Claims Cross-Examiner evaluations

Use these scenarios to confirm the Skill separates claims from evidence, remains conversational and non-adversarial, uses current web research when required, applies calibrated provisional findings, respects sensitive and internal claims, and routes out-of-scope requests correctly.

Each scenario record in `scenarios.md` includes expected activation, first move, required claim type, required definitions, required web research, required source types, required evidence distinctions, expected provisional labels, prohibited behavior, expected output structure, pass criteria, and failure examples.
