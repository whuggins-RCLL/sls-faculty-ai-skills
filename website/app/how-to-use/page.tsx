import { getSkills } from "../lib/skills";
export default function HowToUse(){const skills=getSkills();return <><p className="eyebrow">Guide</p><h1>How to use the Skills</h1><p className="lede">Choose the Skill that matches the scale of your teaching task: the Course and Syllabus Designer plans an entire course, while the Class Session Planner prepares one class meeting or a short connected sequence.</p><section className="grid two">{skills.map((skill)=><article className="card" key={skill.slug}><h2>{skill.name}</h2><p>{skill.summary}</p><p><strong>Try:</strong> {skill.usageExamples?.[0]}</p></article>)}<article className="card"><h2>Upload prior material</h2><p>Attach prior syllabi, lesson plans, readings, slides, or notes when available. If a connector is unavailable, paste the most important text.</p></article><article className="card"><h2>Iterate deliberately</h2><p>Ask for shorter timing, different participation options, a targeted component, or clean Markdown / Word-ready output for review.</p></article></section></>}
