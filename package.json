{
  "private": true,
  "workspaces": [
    "website"
  ],
  "scripts": {
    "dev": "npm --workspace website run dev",
    "build": "npm --workspace website run build",
    "lint": "npm --workspace website run lint"
  }
}
